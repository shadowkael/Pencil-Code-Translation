# Makefile for Client Files

# A few variables

CC=gcc
LIBS=-pthread

SRC_DIR=.
EXE_DIR=../exe

EXES = ${EXE_DIR}/Server

${EXE_DIR}/%: %.o
	${CC} -o $@ $< ${LIBS}

%.o: ${SRC_DIR}/%.c
	${CC} -o $@ -c $<

all:    ${EXES}


clean:
	@rm -f ${EXES}
